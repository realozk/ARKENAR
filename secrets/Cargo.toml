[package]
name = "arkenar-secrets"
version = "0.1.0"
edition = "2021"
description = "Pure secret-pattern detection for Arkenar (no I/O, no async)."
license = "MIT"
repository = "https://github.com/RealOzk/ARKENAR"

[dependencies]
regex = { workspace = true }
