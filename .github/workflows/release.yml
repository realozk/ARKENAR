name: Release Arkenar GUI

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'  # v1.0.1, v1.0.1-beta, v1.1.0-rc1, etc.

permissions:
  contents: write  

jobs:
  build-tauri:
    name: Build GUI — ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false 
      matrix:
        include:
          - platform: windows
            os: windows-latest
          - platform: linux
            os: ubuntu-22.04
          - platform: macos
            os: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Use Node.js LTS
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: gui/pnpm-lock.yaml

      - name: Install frontend dependencies
        working-directory: gui
        run: pnpm install --frozen-lockfile

      - name: Install Linux build dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config

      - name: Install macOS build dependencies
        if: matrix.platform == 'macos'
        run: brew install create-dmg

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: gui
          tagName: ${{ github.ref_name }}
          releaseName: "Arkenar ${{ github.ref_name }}"
          releaseBody: |
            ## Arkenar ${{ github.ref_name }}

            ###  Downloads

            #### GUI (Desktop App)
            | Platform | File |
            |----------|------|
            | Windows  | `Arkenar_*_x64-setup.exe` (NSIS installer, no admin required) |
            | Linux    | `arkenar_*_amd64.AppImage` or `arkenar_*_amd64.deb` |
            | macOS    | `Arkenar_*_universal.dmg` |

            #### CLI (Command Line)
            | Platform | One-command install |
            |----------|---------------------|
            | Windows  | `irm https://raw.githubusercontent.com/${{ github.repository }}/main/install.ps1 \| iex` |
            | Linux / macOS | `curl -sL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh \| bash` |

            See [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for what's new.
          releaseDraft: false
          prerelease: ${{ contains(github.ref_name, '-beta') || contains(github.ref_name, '-rc') }}

  # Job 2: Build the CLI binary for every platform and upload to the same
  # GitHub Release that the GUI job created above.
  build-cli:
    name: Build CLI — ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    needs: build-tauri   # wait for the release to exist before uploading assets

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            asset_name: arkenar-windows-amd64.zip
            archive: zip

          - target: x86_64-unknown-linux-gnu
            os: ubuntu-22.04
            asset_name: arkenar-linux-amd64.tar.gz
            archive: tar

          - target: aarch64-unknown-linux-gnu
            os: ubuntu-22.04
            asset_name: arkenar-linux-arm64.tar.gz
            archive: tar
            cross: true

          - target: x86_64-apple-darwin
            os: macos-latest
            asset_name: arkenar-macos-amd64.tar.gz
            archive: tar

          - target: aarch64-apple-darwin
            os: macos-latest
            asset_name: arkenar-macos-arm64.tar.gz
            archive: tar

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable + target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: ". -> target"
          key: ${{ matrix.target }}

      - name: Install cross (for aarch64-linux)
        if: matrix.cross == true
        run: cargo install cross --locked

      - name: Build CLI binary (native)
        if: matrix.cross != true
        run: cargo build -p arkenar-cli --release --target ${{ matrix.target }}

      - name: Build CLI binary (cross)
        if: matrix.cross == true
        run: cross build -p arkenar-cli --release --target ${{ matrix.target }}

      - name: Package — zip (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $bin = "target/${{ matrix.target }}/release/arkenar.exe"
          Compress-Archive -Path $bin -DestinationPath ${{ matrix.asset_name }}

      - name: Package — tar.gz (Linux / macOS)
        if: matrix.archive == 'tar'
        run: |
          BIN="target/${{ matrix.target }}/release/arkenar"
          tar -czf ${{ matrix.asset_name }} -C "$(dirname $BIN)" "$(basename $BIN)"

      - name: Upload CLI asset to GitHub Release
        run: gh release upload ${{ github.ref_name }} ${{ matrix.asset_name }} --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}